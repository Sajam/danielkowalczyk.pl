# Environment

```
gem install sass -g
gem install node-sass -g
gem install uglify-js -g
```

Editor **Atom** with packages:

```
sass-autocompile https://atom.io/packages/sass-autocompile
uglify-js-autocompile https://github.com/Sajam/atom-uglify-js-autocompile
```
